
#database connection settings
Type: mysql
URL: dnsfilter:rahasiadns@(175.106.8.72:3306)/dnsfilter

#sql commands used in application
SQLCommand:

  GetTotalSites: >
    select COUNT(id) total from dns_dev

  GetMostActive: >
    SELECT base_domain, count(base_domain) total_most_active 
    from dns_dev WHERE WEEKOFYEAR(log_datetime) = WEEKOFYEAR(NOW()) GROUP BY base_domain ORDER BY total_most_active DESC LIMIT 10